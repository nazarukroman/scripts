# Default client IP address (CIDR format)
DEFAULT_ADDRESS="192.18.10.25"

# Configuration directory base path
CONFIG_DIR=/etc/path"

# WireGuard server endpoint (IP:port)
ENDPOINT="ip:51820"

# DNS server for clients
DNS="9.9.9.9"

# Server config path (optional)
# WG_SERVER_CONFIG="/etc/wireguard/wg0.conf"
